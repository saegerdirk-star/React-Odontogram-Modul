# React Advanced Odontogram

## Critical Defaults
- This is a **standalone React library** — it must work independently of DentalQuoteCreator
- Never introduce DentalQuoteCreator-specific dependencies or imports
- Preserve the public API (`OdontogramShell` props interface)
- All changes must maintain backward compatibility with existing integrations

## Architecture
- **React 19.2** + **TypeScript 5.5** — single-page interactive dental charting component
- **Vite 7.3** — dev server and library build
- **Tailwind CSS** — utility-first styling with custom theme via CSS variables (`--odon-*`)
- SVG-based tooth rendering with per-surface interaction

## Key Files
- `src/App.tsx` — main `OdontogramShell` component (exported as default)
- `src/odontogram.ts` — core odontogram state logic and types
- `src/plugin.ts` — plugin system for extending functionality
- `src/status_extras.ts` — additional dental status types (bridges, implants, etc.)
- `src/theme.ts` — theme configuration and CSS variable mapping
- `src/i18n/translations.ts` — multilingual labels (HU, EN, DE, ES)
- `src/utils/numbering.ts` — tooth numbering systems (FDI, Universal, Palmer)
- The dental chart is **two grids, one per arch** (`.tooth-arch.upper-arch` / `.lower-arch` inside `#toothGrid`, built by `buildGrid` in `src/odontogram.ts`; each wrapper is `role="presentation"` so the listbox still owns the tiles). Every column in both arches is its own tooth plus 6 px — one rule, no hand-set values — so every contact is 10 px. The Class I canine relationship falls out of that: the same slack is added on both sides of every contact, so it cancels, and what is left between 13 and 43 is the anatomical difference (the lower six front teeth are 10 mm narrower than the upper six). The tip of 43/33 lands in the embrasure between the upper lateral and the upper canine because the teeth are the widths they are, not because anything was arranged. `ToothSpec.col_px` must agree with `grid-template-columns` (verify.py checks it) or the gum's papillae leave the joints
- `src/assets/teeth-svgs/` — **one template per position** since 2026-08-17: 16 permanent side views (11–18, 41–48), 10 deciduous (51–55, 81–85) and 20 occlusal (11/12/13/41/42/43 anterior plus 14/15/16/17/18/44/45/46/47/48/54/55/84/85 `_occl`), mapped by `TOOTH_TEMPLATE` / `PRIMARY_TEMPLATE` / `OCCLUSAL_TEMPLATE` in `src/odontogram.ts`. The opposite side is still the same file mirrored (21 from 11, 31 from 41, 61 from 51, 71 from 81) — nothing is shared ACROSS the jaws any more. Every one of them is GENERATED: `tools/toothgen/redraw_alle.py` and `redraw_occl.py` insert Dirk's drawn contour and pulp into a DONOR template that supplies the ~200 clinical layers. Never hand-edit one. The permanent side views are inlined as `?raw` imports; the deciduous and occlusal ones load on demand (`import.meta.glob`), so a chart pays only for the artwork it mounts
- `tools/toothgen/` — **three stages, two contracts.** `build.py` (+ `occlusal.py`) generates the Schumacher-derived templates into `tools/toothgen/spender/` — not versioned, reproducible, `npm run toothgen:spender`. Those are the DONORS. `redraw_alle.py` / `redraw_occl.py` (`npm run toothgen:redraw`) then insert Dirk's drawn contour and pulp and write what actually ships. **The rule the whole redraw arc converged on: what is drawn gets INSERTED; only what nobody draws is warped.** It is not unlimited — deriving the veneer by the same pattern made it worse (see `veneer_aus`). `redraw_plan.py` holds both target→drawing→donor tables and nothing else, so a check need not import a generator. `verify.py` measures the DONORS against `spec.py` and its frozen digests (unchanged by the redraw); `verify_redraw.py` is the contract for the shipped set — layer inventory identical to the donor, one continuous contour, lumen inside the root, the implant body not stretched, the gum drawn for the column the tooth actually stands in, and the occlusal plane on one line. `npm run toothgen:verify` runs both plus `check_roundtrip.py`. `spec.py` records the per-class anatomy (root count, root fraction, crown ratio, furcation), `verify.py` re-measures the generated assets against it and against frozen geometry digests, `check_roundtrip.py` proves path serialization is lossless. `crown_width` (outline extent) and `silhouette_width` (tooth material) are DIFFERENT measurements and must not be swapped — a crown width is the first, the root that has to hold a canal is the second. `fillings.py` stretches the mesial and distal filling shapes until they meet the occlusal one, so MO/OD/MOD draws as ONE restoration (`verify.py` fails if a pair stops being one connected shape); the buccal shape is deliberately left separate. `gum.py` DRAWS `gum-base` and `bone-base` outright, in final coordinates, instead of warping them out of the sources — the only way to make the gingiva read as one line across the arch, since a papilla is shared between two teeth and two hand-drawn halves cannot agree on its height. Its papilla sits half a column plus half a grid gap from the tooth's centre, so two neighbours meet on the same point; `ToothSpec.col_px` holds that column width and `verify.py` checks it against `grid-template-columns` in `src/index.css`. **The THIRD stage inserts the surfaces a finding is charted on**, after the contour is in place, because they are derived FROM the drawn contour and cannot be warped out of a donor: `fuellflaechen.py` derives the mesial/distal/occlusal (incisal on an anterior) boundaries from Dirk's drawn boxes into layer `4 FUELLFLAECHEN (abgeleitet)` — a hand-drawn shape always beats a derived one; `fuellflaechen_einsetzen.py` rasterises those regions and writes them into the shipped template (serialise with TWO decimals — the chain re-serialises at `prec=2`, and three decimals fail `check_roundtrip.py`); `kauflaechen.py` does the same for the occlusal view, cutting the table along the drawn fissures and projecting the buccal WIDTH back onto the side view; `halsbaender.py` draws `caries-root`/`caries-subcrown` as analytic horizontal slabs at the neck rather than warping them; `draufsicht.py` reads an anterior top-view drawing BY CONTENT (largest closed path = outline, open paths = lines), checks Dirk's `v`/`m` orientation marks against the documented convention, and normalises it to the side view's crown width as `<tooth>_occl_norm.svg`. Supporting modules, called by the above rather than run directly: `roots.py` (root topology before the common transform), `svgpath.py` (path parse and serialise), `graft.py`, `hoecker.py` (raster flood-fill planar subdivision of a drawn occlusal table), `redraw_apply.py` (the insertion itself)
- `src/assets/icon-svgs/` — UI icons

## Tooth State Model
Each tooth has:
- `toothSelection`: tooth-base | missing | implant | pontic
- `toothSubstrate`: natural | radix | broken | crownprep
- `restorationType`: none | crown | inlay | onlay | veneer | bridge (implant-gated crowns/bridges compose with an implant connector layer). Restoration options are gated by tooth kind via `restorationOptions()` (`src/registry/restorations.ts`): an implant tooth offers only crown/bridge, plus the five `prosthesis` attachment entries; a missing/gap tooth (`toothSelection === "none"`) offers only a bridge pontic, plus the two removable-denture `prosthesis` entries; a `radix` substrate hides the restoration control entirely (`restorationRowHidden()` in `src/odontogram.ts`) — no restoration can be authored on a root remnant. A `bridge` restoration renders both the crown cap AND the saddle-connector layer via `composeRestorationLayers()`; the multi-tooth bridge-span overlay connector is arch-aware (mirrored saddle-Y fraction for the lower arch, `SADDLE_Y_FRACTION_LOWER` in `src/bridgeOverlay.ts`), and applying a bridge via a Statuses preset explicitly re-triggers the overlay recompute
- `restorationMaterial`: none | emax | gold | gradia | zircon | metal | metal-ceramic | telescope | temporary
- `prosthesis`: none | healing-abutment | locator | locator-denture | bar | bar-denture | removable-partial | removable-full — orthogonal implant-attachment / removable-denture axis, surfaced as "Kivehető:" entries in the combined restoration dropdown (gated per tooth kind — see `restorationType` above)
- `crownLeakage`: boolean — marginal-leakage finding, shown only when `restorationType` is crown or bridge. Both the tooltip (`getStateSummary`) and whole-mouth summary (`getOdontogramSummary`) gate this line on the exact same predicate as the `#crownLeakageRow` control (`!restorationRowHidden(state)` AND crown/bridge), so a tooth with a stale crown/bridge payload but a hidden restoration row (radix/milktooth/extraction/under-gum) never shows a leakage line the control itself doesn't display
- `endo`: none | endo-medical-filling | endo-filling | endo-filling-incomplete | endo-glass-pin | endo-metal-pin — mutually exclusive with `pulpDx`; both are surfaced through one merged "Pulp / Endo status" selector (vital vs. treated groups), and setting `endo` to a treated value normalizes `pulpDx` to `normal` (suppressing the diseased-pulp glyph)
- `pulpDx`: normal | reversible-pulpitis | irreversible-pulpitis | necrosis — AAE pulp diagnosis (replaced the retired `pulpInflam` boolean); mutually exclusive with `endo` (see above); `reversible-pulpitis` renders a reduced pulp glyph
- `pulpLatin`: none | pulpa-sana | hyperaemia-pulpae | pulpitis-acuta-serosa | pulpitis-acuta-purulenta | pulpitis-chronica-clausa | pulpitis-chronica-ulcerosa | pulpitis-chronica-hyperplastica | necrosis-pulpae | gangraena-pulpae — practical-Latin pulp subtypes, surfaced by the pulp picker only when the `pulpDetailLevel` setting (`simple` | `aae` | `latin`, default `aae`) is `latin`. `setPulpDetailLevel()` calls `notifyStateChange()`, so switching the level live-refreshes the whole-mouth summary and every tooth's tooltip immediately instead of staying stale until the next tooth edit
- `apicalDx`: normal | symptomatic-apical-periodontitis | asymptomatic-apical-periodontitis | acute-apical-abscess | chronic-apical-abscess | condensing-osteitis — drives the periapical glyph directly, decoupled from the `mods.inflammation` modifier. The `mods.inflammation` toggle (`syncInflammationModVisibility()` in `src/odontogram.ts`) is shown ONLY for missing (`toothSelection === "none"`) and extraction-socket (`no-tooth-after-extraction`) teeth — it is hidden on present teeth (where `apicalDx` drives the glyph) and on implants (where `periImplant` drives it instead)
- `periapicalType`: none | granuloma | cyst — periapical lesion subtype qualifying the `apicalDx` glyph, offered by the picker only under symptomatic/asymptomatic apical periodontitis; the legacy `abscess` value is still accepted/stored (folded into `apicalDx` on import) but no longer authorable, since it duplicates the apical diagnosis
- `resorptionType`: none | internal | external-cervical — replaced the retired `rootResorption` boolean
- `periImplant`: none | mucositis | peri-implantitis-mild | peri-implantitis-moderate | peri-implantitis-severe — implant-only peri-implant status (2018 World Workshop staging); `mucositis` reuses the existing periodontal gum glyph, `peri-implantitis-*` additionally activates the `peri-implant-bone-loss` layer at severity-scaled opacity (0.4/0.7/1.0); retires the implant `mods.inflammation` periapical glyph (missing/extraction-socket teeth are unaffected) and hides the inflammation/parodontal modifier checkboxes on implants
- `mods.mobility` (M1/M2/M3 grades): the mobility control is hidden/disabled on implant teeth (`mobilityRowHidden()`/`mobilityDisabled()` in `src/odontogram.ts`) — implants don't carry periodontal mobility
- `caries`: array of surface identifiers (mesial, occlusal, distal, buccal, lingual)
- `fillingSurfaces`: array of filled surfaces
- `rootCaries`: none | active | arrested | active-cavitated — wires the `caries-root` artwork layer on a present tooth, at an opacity driven by severity (`active` 0.5 / `arrested` 0.7 / `active-cavitated` full)
- `caries`/`fillingSurfaceMaterials`/`cariesSeverity`: caries is a per-surface **state machine**, not two independent fields. `cariesSeverity` (0-6) is the single unified per-surface severity — replacing the old separate `cariesDepths` (ICDAS) and `secondaryCaries` (CARS) fields. A surface in `caries` with **no** filling on it renders as primary caries (`caries-{surface}`, ICDAS-tiered opacity from `cariesSeverity`); the same surface **with** a filling present renders as recurrent caries instead (`subcaries-{surface}`, CARS-scored opacity from `cariesSeverity`) — a surface is never both at once. A contextual popup shows only the severity group relevant to the surface's current state (primary ICDAS-depth group when unfilled, CARS group when filled)
- `radiographicDepth`: per-surface none | E1 | E2 | D1 | D2 | D3 — radiographic caries depth, independent of the visual ICDAS/CARS severity scale (`cariesSeverity`); surfaced as a `data-radio` badge attribute
- `fillingDefect`: per-surface none | marginal | fracture | wear — filling-defect finding on a DIRECT restoration (marginal defect/overhang, fracture/chip, or worn/deficient material), independent of recurrent caries; gated to surfaces present in `fillingSurfaceMaterials` (only a filled surface can carry a defect). Authored via a left-side per-surface indicator on the Fillings card (mirrors the right-side caries-depth indicator); renders on the chart by activating the existing `defect-{surface}` artwork layer; shown in the tooltip and the whole-mouth fillings summary, labeled explicitly (`fillingDefect.label` prefix) the same way recurrent caries is labeled on the Caries summary line. Payload version 2.7
- `wearEdge`: none | attrition | erosion — incisal/occlusal tooth-wear type; `wearCervical`: none | abrasion | abfraction | erosion — cervical tooth-wear type. Replaced the two on/off bruxism-wear flags (`bruxismWear`/`bruxismNeckWear`); authored via two dropdowns on the wear row. Render the existing `tooth-bruxism-wear`/`tooth-bruxism-neck-wear` layers (no new SVG), gated on `toothSelection === "tooth-base"` + `restorationType === "none"` + `toothSubstrate === "natural"`. Shown in the tooltip and a dedicated whole-mouth "Wear" summary section. Payload version 2.8; a legacy edge-wear flag migrates to `attrition`, a legacy cervical-wear flag migrates to `abrasion`
- `discoloration`: none | tetracycline | fluorosis | nonvital | extrinsic | other — per-tooth discoloration cause. Tints the shown natural crown's fill (`tooth-base` or `milktooth-base`, whichever is active) a cause-specific colour at render time; gated on a natural, unrestored tooth-base or milk tooth (`toothSelection` tooth-base or milktooth + `restorationType === "none"` + `toothSubstrate === "natural"`). Fill is not part of the SVG-fingerprint parity check (only id/opacity/class are), so the tint is parity-safe. Shown in the tooltip and a dedicated whole-mouth "Discoloration" summary section. Payload version 2.9; additive, no migration needed
- `orthoAppliance`: none | bracket | band; `orthoDrift`: none | mesial | distal; `orthoVertical`: none | extrusion | intrusion (extrusion renders an arrow-up glyph, intrusion an arrow-down glyph); `orthoRotation`: boolean — per-tooth orthodontic charting axes. Reuse the dormant v2.5.0 ortho artwork (no new SVG); gated on a present natural tooth (`toothSelection` tooth-base or milktooth — the shared `orthoAllowed` predicate). Shown on the chart, in the tooltip, and a new whole-mouth "Orthodontics" summary section. Payload version 2.10; additive, no migration needed
- Wear (`wearEdge`/`wearCervical`) and `discoloration` each have an app-level session detail level (`ToothDetailLevel`: `simple` | `complex`, default `complex`), set via Settings → Tooth details. Simple mode swaps the type/cause dropdown for a yes/no toggle per finding (wear on → `attrition`/`abrasion`, discoloration on → `other`); complex mode (default) shows the full dropdown. The setting only toggles which control is visible/derives its checked state from the stored value — it never mutates `state`, so the stored value survives switching levels. No payload/render/FHIR change.
- `SurfaceNotation`: `simple` | `full` (default `full`) — app-level session setting (`getSurfaceNotation`/`setSurfaceNotation` in `src/odontogram.ts`), set via Settings → Tooth details → "Surface notation". Drives `surfaceLetter()`/`surfaceLabelKey()`, which resolve the DISPLAY letter/caption for a caries/filling surface given the tooth's arch (`isUpperTooth`) and anterior/posterior position (`isAnteriorTooth`); shared by the whole-mouth summary AND both the caries and filling-defect surface pickers (letter + caption). Mapping — FULL: occlusal → "I"/incisal on an anterior tooth (else "O"/occlusal); buccal → "L"/labial on an anterior tooth (else "B"/buccal); lingual → "P"/palatal on an upper tooth (else "L"/lingual — a lower-anterior tooth therefore shows "L" for both buccal and lingual, disambiguated by cross-position + caption); mesial → "M", distal → "D", subcrown → "SC" (unaffected by the setting). SIMPLE: always the tooth-independent B/M/O/D/L/SC set. The stored `surface` key (e.g. `"occlusal"`, `"buccal"`, `"lingual"`) is never affected — only the displayed letter/label changes.
- Settings → "Panels" tab (`settings.panels.*`; `showStatusCard`/`onShowStatusCard`, `showOrthoCard`/`onShowOrthoCard` in `App.tsx`): independently toggles whether the whole-mouth Statuses card and Orthodontics card render (both default visible). Session-level UI state only — no payload/render/FHIR change to per-tooth data.
- Settings tabs are `general | panels | toothDetails | caries | pulpa | notes` (`SETTINGS_TABS` in `src/SettingsModal.tsx`). The former separate "Secondary caries" tab is merged into the "Caries" tab, with the CARS (secondary-caries) control positioned directly above Radiographic depth.
- `extractionPlan`, `crownNeeded`, `crownReplace`: boolean flags
- The per-tooth tooltip (`getStateSummary`) and the whole-mouth summary panel (`getOdontogramSummary`) surface all clinical axes above — pulp/apical diagnosis (+ lesion subtype), root resorption, peri-implant status, graded root caries, calculus, crown marginal leakage, fracture, contact loss, and typed edge/cervical wear — not just what is drawn on the chart. The panel groups pulp/apical/resorption/peri-implant findings into a dedicated `diagnoses` summary section, adds a dedicated `wear` summary section, and caries surfaces a coarse severity qualifier (superficial/moderate/deep) alongside the per-surface state.

## Periodontal Data (perio sub-record)
- Each tooth state carries a `perio` sub-record — `{ pd: Map<site,mm>, gm: Map<site,mm>, bop: Set<site>, sup: Set<site> }` over the six standard sites `PERIO_SITES = ["MB","B","DB","ML","L","DL"]` (buccal row, then lingual row). Kept SEPARATE from the 5-surface caries maps (different site convention). First sub-project (P1) of the periodontal-parameters arc; later sub-projects add the perio-chart grid UI, SVG recession/furcation rendering, and 2017 staging/grading.
- **Semantics:** absence = "not charted", NOT zero. A site is charted once its `pd` is set; `gm` (gingival margin vs CEJ, SIGNED: recession +, coronal/pseudopocket −) defaults to 0 when `pd` is set but `gm` unset. `setPerioSite(toothNo, site, {pd?,gm?,bop?,sup?})` validates + clamps (pd int 1–15, gm int −10…+20); `pd` null/`<1` un-charts the site (atomically clears pd/gm/bop/sup for it — no orphans).
- **Derived, never stored:** `CAL = pd + (gm ?? 0)` (`getToothCal`); recession = `max(gm,0)`; pseudopocket = `max(−gm,0)`; whole-mouth `%BOP = (charted sites ∩ bop) ÷ charted sites` (can't exceed 100 — BOP on an un-charted site is ignored). `getPerioSummary()` returns charted/bleeding counts, %BOP, worst CAL, max PD. Public API: `setPerioSite`/`getToothPerio`/`getToothCal`/`getPerioSummary`/`getPerioChart`. `getPerioChart`/`getToothPerio` are OMIT-WHEN-EMPTY (uncharted sites have no key — default them like `radiographicDepth`).
- Payload version **2.12** (additive; `perio` omitted from a tooth's serialized record when no site is charted, so a no-perio case is byte-identical apart from the version). Dual-state aware (lives on the tooth state → participates in status/plan charts + the R2-B `getPlanChanges()` diff via one summary-level `perio` DIFF_AXES entry). No SVG render yet (that's a later sub-project) → SVG-fingerprint parity byte-identical.
- **Graphical perio chart (`src/PerioChart.tsx` + `src/perioGraphic.ts`):** the perio chart is drawn like a real perio chart — the teeth in a continuous **occlusal-to-occlusal** arch (upper crowns down, lower up; per-arch `archOrientTransform` flip about the CEJ baseline, curves + mm grid ride the same oriented row groups so they align), reusing the existing artwork, with a CEJ line + a **numbered mm guide grid** (`buildMmGridLayer`, lines at `ROW_BASELINE_Y + n·PERIO_MM_PX`, behind teeth, aria-hidden) + a pocket/margin curve over them, number rows in aligned columns, and a summary. The arch SVG is **responsive** (no fixed width/height — `viewBox` + `width:100%`; the number grid keeps fixed-px columns so a narrow window scrolls). **Implant teeth** render the template's `#implant-base` layer instead of `#tooth-base` (`getToothBaseGroupFromCache(..., {implant})` + a per-template `IMPLANT_CEJ_Y` anchor; `isToothImplant` reads the active chart). Furcation/plaque status→plan changes surface in the R2-B "What changes" diff (summary-level `DIFF_AXES` entries). `src/perioGraphic.ts` holds the DOM-free logic: `getToothBaseGroup(toothNo)` extracts ONLY the `#tooth-base` layer from a tooth's template (its OWN independent template cache — separate `fetch`+`DOMParser`, architecturally disjoint from the live grid's `tplCache`, so it can never corrupt the real chart; clones before transforming; drops `gum-base`/`bone-base`/`tooth-healthy-pulp`/`tooth-base-beauty`/`milktooth-base`), applies per-position size (FDI last digit 2→80% width, 3→taller root, rest as-is) + the horizontal `mirror`, aligned on a common **CEJ baseline** via per-template `CEJ_Y` anchors (measured from `gum-base`'s coronal edge as a cervical proxy); the palatal row is the buccal row wrapped in a vertical mirror. `perioCurve(sites, {cejY, mmPx, siteX})` is a pure helper → `marginY = cejY + gm*mmPx`, `pocketY = marginY + pd*mmPx`, `null` for uncharted (line gaps); the overlay draws the red CEJ line + margin/pocket polylines + a filled band, reusing the SAME `archToothLayout` per-tooth x/width so it tracks the teeth. Live + dual-state via `onStateChange`.
- **Cairo recession + CEJ/root-concavity (PG-C):** `getToothRecessionType(toothNo)` derives the Cairo type (`none`/`rt1`/`rt2`/`rt3`) from `getToothCal` — only when the buccal margin `gm.B > 0`, comparing `max(CAL[MB],CAL[DB])` (interproximal) vs `CAL[B]` (buccal): RT1 interprox <1mm, RT2 interprox ≤ buccal, RT3 interprox > buccal (pure, no storage; surfaced as the `"cairo"` overlay layer + tooltip/summary). `cejVisibility` (`none`/`detectable`/`not-detectable`) and `rootConcavity` (`none`/`mild`/`deep`) are per-tooth **registry enum axes** (`registry/axes.ts` + `fieldMappings.ts` 1:1 + `codesystems.ts` local codes; NO `svgLayer` → no odontogram render → parity byte-identical); gated setters `setCejVisibility`/`setRootConcavity` (through the DS-1 gate), omit-when-`none` serialize, payload **2.14**, FHIR via the declarative axis path, Dental Chart cycle-button rows + info popups + summary/tooltip.
- **Graded indices + mucogingival findings (PG-D):** five additional per-tooth axes. **PI** (`getPlaqueIndex`/`setPlaqueIndex`) and **GI** (`getGingivalIndex`/`setGingivalIndex`) — Silness-Löe Plaque Index / Löe-Silness Gingival Index, per-surface graded `0`-`3` over the same 4 O'Leary surfaces as `plaque` (mesial/distal/buccal/lingual), stored as `pi`/`gi` `Map<surface,grade>` (grade `0` clears the entry — never distinct from "never charted", same "absence = not charted" convention as every other graded axis). **KG** (`getKeratinizedWidth`/`setKeratinizedWidth`) — keratinized gingiva width, a single per-tooth BUCCAL mm scalar (integer, clamped 0-15 via `clampKg()`; `null` = not charted, never a stored 0), deliberately NOT per-site/per-surface unlike PI/GI or the 6-site perio-probing record. **GT** (`getGingivalThickness`/`setGingivalThickness`, `gingivalThickness` field, `VALID_GINGIVAL_THICKNESS`) — gingival thickness phenotype (`unknown`/`thin`/`medium`/`thick`, default `unknown`) and **Miller class** (`getMillerClass`/`setMillerClass`, `millerClass` field, `VALID_MILLER_CLASS`) — Miller recession classification (`none`/`i`/`ii`/`iii`/`iv`, default `none`) are per-tooth **registry enum axes** (`registry/axes.ts` + `fieldMappings.ts` 1:1 + `codesystems.ts`, mirroring `cejVisibility`/`rootConcavity`'s PG-C pattern exactly). All five interactive setters route through the DS-1 gate (`gateToothEdit`); NONE has an `svgLayer` → no odontogram render → parity byte-identical. PI/GI/KG serialize to FHIR via a bespoke path (`src/fhir/toFhirPerio.ts`, engine-local codes — no dedicated LOINC, same treatment as per-site BOP/plaque) as additional components on the existing per-tooth periodontal-panel `Observation`; GT/Miller serialize via the declarative axis path like `cejVisibility`/`rootConcavity`. Payload **2.15**, Dental Chart rows (PI/GI heat-bucketed like the mm ramps; KG/GT/Miller cycle-button/value rows) + info popups + tooltip/summary.
- **Peri-implant Mombelli indices (PG-E):** two implant-only per-surface graded axes. **mPI** (`getPeriImplantPlaque`/`setPeriImplantPlaque`) and **mBI** (`getPeriImplantBleeding`/`setPeriImplantBleeding`) — Mombelli modified Plaque Index / modified Sulcus Bleeding Index, per-surface graded `0`-`3` over the same 4 surfaces as PI/GI (mesial/distal/buccal/lingual), stored as `mpi`/`mbi` `Map<surface,grade>` sharing the same `setSurfaceGrade`/`getSurfaceGrade` machinery as PI/GI (grade `0` = not charted, same convention). **Implant-gated end-to-end**: the setter no-ops on any tooth whose `toothSelection !== "implant"` (guard runs before the DS-1 gate, so a gated call marks nothing and fires no state-change notification), and the Dental Chart cell's `disabled` predicate is `readOnly || !isToothImplant(toothNo)` — a deliberate departure from the shared `isPerioRowHidden` gate every other perio row uses, since that gate hides implants (`isToothPresent` excludes them) exactly where mPI/mBI need to be active. Hydrate is intentionally NOT implant-gated (non-interactive path, same tolerance policy as every other axis) — only the setter enforces the gate. NEITHER axis has an `svgLayer` → no odontogram render → parity byte-identical. FHIR via the bespoke `src/fhir/toFhirPerio.ts` path (local codes `mod-plaque-index-mombelli`/`mod-bleeding-index-mombelli`, no dedicated LOINC — same treatment as PI/GI) as additional components on the existing per-tooth periodontal-panel `Observation`. Payload **2.16**. Also consolidates the Dental Chart overlay's whole-mouth read-out (`overlayReadout`) to cover `pi`/`gi`/`kg` in addition to `bop`/`plaque`/`mpi`/`mbi` — closing a PG-D-era gap where only bop/plaque had a read-out.
- **Case-level metadata object (P4a):** the engine's FIRST case-level object — a single shared block (type `CaseMeta` in `src/odontogram.ts`), NOT per-tooth and NOT dual-state (both `charts.status` and `charts.plan` payloads carry the same object), mirroring the top-level `globals` payload key. Carries patient **age** (`0`-`120`), **smoking status** (`unknown`/`never`/`former`/`current` + cigarettes/day `0`-`99`), **diabetes status** (`unknown`/`none`/`present` + HbA1c `3.0`-`20.0`, one decimal), and two perio-outcome summary stats: **teeth lost to periodontitis** (`0`-`32`) and **max radiographic bone loss %** (`0`-`100`). API: `getCaseMeta()`, `setCaseAge`/`setSmokingStatus`/`setCigarettesPerDay`/`setDiabetesStatus`/`setHba1c`/`setToothLossPerio`/`setMaxRblPercent`, `resetCaseMeta()` — all numeric setters clamp/no-op on non-finite input, `null` explicitly clears; enum setters validate against a fixed set and ignore invalid values. Serializes omit-when-empty to the payload's top-level `case` key (absent entirely when every field is at its default); hydrated via the shared `hydrateImportedCharts` data-path used by both `importStatus()` and the test seam. Wired into the reset-all path (blank-slate reset, like edentulous) alongside `__resetChartStateForTest`. Surfaced as a collapsible panel on the Dental Chart (`src/PerioChart.tsx`, shared `gridBody` so it renders in both `inline`/`popup` chrome) and as a compact labelled fragment appended to `getOdontogramSummary()`'s `periodontalText` (`caseContextSummaryFragment()`) whenever the case isn't empty — only actually-charted fields are mentioned. Payload **2.16 → 2.17** (additive). **No FHIR yet** — this is chart-independent case context only; the periodontal staging/grading classification (and its FHIR `Condition`/BNO mapping) is the P4b sequel. NEITHER a render axis nor per-tooth → SVG-fingerprint parity byte-identical.
- **2017 periodontal classification + first FHIR Condition (P4b):** completes the round-2 periodontal arc. A dependency-free pure derivation core (`src/perioClassification.ts`, `derivePerioClassification`) computes `diagnosis`/`stage`/`grade`/`extent` from an already-reduced `PerioDerivationInput` — per-tooth worst interdental/buccal-oral CAL + max PD, whole-mouth %BOP + max furcation, and the P4a `PerioMetaInput` (age/RBL%/tooth-loss/smoking/diabetes) — with NO imports from `odontogram.ts` and no `Date`/`Math.random`, so it is deterministic and callable from either live state or a serialized payload. Two thin adapters feed it: `buildDerivationInputFromState()` in `odontogram.ts` (live chart → UI) and a payload-side equivalent in `src/fhir/toFhirPerio.ts` (exported JSON → FHIR) — one-directional dependency, no duplicated derivation logic. Four per-axis clinician overrides live on `caseMeta` (`diagnosisOverride`/`stageOverride`/`gradeOverride`/`extentOverride`, each a valid enum value or `null`; NOT DS-1 gated, same treatment as the rest of `CaseMeta`) via `setDiagnosisOverride`/`setStageOverride`/`setGradeOverride`/`setExtentOverride`; `getPerioClassification()` returns the final per-axis value (override ?? derived) alongside the untouched `derived` result and a per-axis `overridden` flag, surfaced as a Dental Chart classification panel and a summary-line fragment. `appendPerioCondition` (`src/fhir/toFhirPerio.ts`) emits the engine's **FIRST FHIR `Condition`** — BNO/ICD-10 K05.3 (chronic periodontitis) / K05.2 (molar-incisor pattern, checked before the generic periodontitis code) / K05.1 (chronic gingivitis) — with type-differentiated `stage[]` entries (periodontal-stage/-grade/-extent, each only when that axis is applicable/non-indeterminate) and `evidence[]` referencing smoking-status and HbA1c Observations (emitted only when those case fields are charted); emits NOTHING for a "health" diagnosis. Payload **2.17 → 2.18** (additive — the four override fields). Neither the derivation nor the overrides have an `svgLayer` → SVG-fingerprint parity byte-identical. **Important:** this is the FIRST sub-project to touch the FHIR golden fixture, but the delta is empty — all four parity fixtures carry no perio/case data, so they derive `diagnosis: "health"` and `appendPerioCondition` emits nothing; a diseased case DOES emit a Condition (covered by `p4b-condition-fhir.test.ts`), it's just that none of the golden fixtures happen to be diseased.
- **Index switcher + overlays (PG-B):** a radio-style switcher (`#perioOverlaySwitch`) in the Dental Chart header sets a runtime `perioOverlayLayer` (`getPerioOverlayLayer`/`setPerioOverlayLayer`; default `"none"`) that repaints a highlight overlay on the teeth — `pd`/`cal`/`gr` heat ramps (`pdCalHeatBucket`/`recessionHeatBucket`, GR only `gm>0`), `bop` dots, `plaque` marks, `pd5`/`pd6` deep-pocket heat. All display-only over existing data (`getToothPerio`/`getToothCal`/`getPerioSummary`/`getToothPlaque`); the overlay `<g>` (aria-hidden) reuses the SAME `archToothLayout` + oriented row groups as the curve/mm-grid (one geometry), so highlights land on the right sites both arches. `%BOP`/`PI%` read-outs from `getPerioSummary`. Row labels carry an "i" info button (`.perio-info-btn`) → a lightweight `.perio-info-popover` (`perio.info.*`, capture-phase Esc/click-away dismiss so it doesn't close the overlay dialog; z-index 165 above the overlay 160, below the DS-1 confirm 200).
- **Presentation (toggle vs popup):** `perioViewMode: "toggle" | "popup"` (default `toggle`; `getPerioViewMode`/`setPerioViewMode`, a Settings option). Toggle mode = an `Odontogram | Periodontal Status` view toggle (`#appViewToggle`, internal id/DOM codename still `dentalChart`) that hides `.chart-column` via CSS (odontogram NEVER unmounted → parity byte-identical) and renders `<PerioChart inline/>`; popup mode = the original overlay. Either way the perio content is the same and `PerioChart` stays a **separately-invocable** component (named export + `openPerioOverlay`/`closePerioOverlay`/`isPerioOverlayOpen`) for host integration — the housing is the parent's choice.
- **Perio-view layout overhaul (UI-1):** while the toggle view is active, the right `<aside className="panel">` is view-gated and no longer renders the odontogram Controls panel — it renders `PerioSidebar` (`src/PerioSidebar.tsx`, extracted out of the chart body, kept DOM ids/classes so both the toggle and popup housings render byte-identical markup), which bundles the Patient-data card, the 2017 classification panel, and the whole-mouth periodontal summary into one perio-context sidebar; both `PerioChart` and `PerioSidebar` subscribe to `onStateChange` independently, so either can re-render without the other re-mounting. The index rows (PD/GM/CAL/BOP + mobility + furcation + plaque) show full index names instead of abbreviations, in larger cells for touch targets. The arch SVG's fixed size was replaced by a `ResizeObserver`-driven fill-scale that reads the container width and scales through the SAME `archToothLayout` geometry the curve/mm-grid/overlay all already share — one geometry, so nothing drifts out of alignment when it scales. Purely presentational: no `odontogram.ts` derivation/serialization change, no `perioClassification.ts` change, no `fhir/*` change — SVG-fingerprint, FHIR-golden, and roundtrip-golden fixtures are byte-identical (v1.46.0). The sequel (UI-2, below) adds a Settings → Periodontal tab with per-index show/hide toggles and a canonical-vs-translated index-name option.
- **Settings Periodontal tab + per-index visibility/naming (UI-2):** a new Settings tab (`id: "periodontal"` in `SETTINGS_TABS`, `src/SettingsModal.tsx`) with 16 per-index show/hide toggles for the perio-chart rows — grouped Pocket (PD/GM/CAL/BOP) / Hygiene (Plaque/PI/GI) / Mucogingival (CEJ visibility/Root concavity/KG/GT) / Support (Furcation/Mobility/Miller class) / Peri-implant (mPI/mBI), `PERIO_ROW_GROUPS` — each row with its own info description, default all-visible; a companion translated-vs-canonical index-name select (`PerioIndexNameMode`: `"translated"` default / `"canonical"`). `perioRowVisibility: Record<PerioRowId, boolean>` and `perioIndexNameMode` (`getPerioRowVisibility`/`setPerioRowVisibility`, `getPerioIndexNameMode`/`setPerioIndexNameMode` in `src/odontogram.ts`) are **app-level session flags — like `perioViewMode` — never part of the export payload** (not read/written by `collectExportPayload`/`getPlanChart`/hydrate, no `svgLayer`, so SVG-fingerprint/FHIR/roundtrip goldens stay byte-identical; payload stays 2.18). Deselecting a row hides it from the perio-chart grid (the Dental Chart consults `getPerioRowVisibility()` per row before rendering it); the chart renders every index row's label via `indexName(rowId)` (`src/perioIndexNames.ts`), which honors `perioIndexNameMode` — `"translated"` resolves the row's existing localized `t(...)` key (unchanged i18n), `"canonical"` returns a FIXED English/Latin scientific name from `CANONICAL_INDEX_NAMES` regardless of the active UI language. The "i" info-button tooltips (`perio.info.*`) are UNRELATED to this mode and always stay localized — every tooltip call site keeps passing a plain `t("perio.info.<field>")`, never `indexName()`.
- **Assessment-status authoring UI (odontogram-vnt):** the odontogram-2vd assessment axis (`assessed` / `not-assessed` / `unmeasurable` / `not-applicable` per periodontal axis + measurement point) is now authorable from the perio chart. `perioAssessmentMode` (`getPerioAssessmentMode`/`setPerioAssessmentMode` in `src/odontogram.ts`) is a THIRD app-level session flag alongside `perioRowVisibility`/`perioIndexNameMode` — never serialized, no `svgLayer`, so SVG-fingerprint/FHIR/roundtrip goldens stay byte-identical and the payload stays **2.21**. Default OFF; a `#perioAssessmentToggle` checkbox in the chart header (both chromes) flips it, and the flag is part of `visibilitySig` so a flip REBUILDS the grid (`buildArch` reads it once per build, exactly like row visibility). On, every visible index row for an axis in `PERIO_ASSESSMENT_AXES` gains a companion row (`appendAssessmentRow`) of `.perio-fullgrid-assess` cycle buttons — one per measurement point (`assessmentQualifiers`: site / surface / furcation entrance / whole tooth), id `perio-fg-assess-{tooth}-{axis}-{qualifier|tooth}`, cycling not-assessed → assessed → unmeasurable → not-applicable via `setAssessmentStatus` (the 2vd setter — no second mutation path, so the capability matrix and the DS-1 gate apply unchanged). CAL has no assessment row (derived, never charted). A cell is disabled on `readOnly`, on `!perioAxisApplies` (the matrix is REFLECTED, not fought), and — via the new read-only `isAssessmentCharted(toothNo, axis, qualifier)` export — wherever the point already holds a measurement, since `getAssessmentStatus` resolves a measurement ahead of any recorded status. Recorded statuses surface in the tooltip (one line per status, grouped, via `assessmentSummaryLines`) and in `getOdontogramSummary().periodontalText` (per-status counts, `assessmentSummaryFragment`); both skip a point that holds a value and both gate on the same matrix, so a tolerant import can never surface a status the control itself refuses. `PERIO_INDEX_LABEL_KEYS` moved into `odontogram.ts` (was `TRANSLATED_INDEX_KEYS` in `perioIndexNames.ts`) so the chart's row labels and the summary lines name an index from ONE table.
- **Perio chart: central index band + anatomical diamond tiles (UI-3a):** a UI-only redesign of the arch graphic. Each arch's tooth graphic is now **two separate SVGs** (`buildBuccalArchSvg`/`buildPalatalArchSvg` in `src/perioGraphic.ts`, replacing the retired single-arch `buildArchGraphic`/`archOrientTransform`/`isUpperArch`) sharing the same `archToothLayout` x/width geometry as the curve/mm-grid/overlay — a buccal aspect above, a palatal/lingual aspect below — both drawn with a **uniform crowns-to-band orientation** (crowns face the central band, roots face outward toward the tooth-number rows) instead of the old occlusal-to-occlusal single arch. Between them sits a **central perio index band** (labeled `▲ Buccal … Lingual/Palatal ▼`, `perio.band.buccal`/`perio.band.lingual` i18n keys) carrying the indices that apply to the whole tooth rather than one aspect: **Miller class** moved to the very top of the arch (near the buccal aspect, previously a per-tooth-row entry lower in the grid), and **Plaque/PI/GI/mPI/mBI** now render as one **anatomical diamond tile** per tooth (`diamondGridArea()` in `src/PerioChart.tsx`; buccal tip up, lingual tip down, mesial/distal on the middle row) instead of four separate cross-shaped surface buttons — mesial/distal swap sides per `mesialOnLeft(toothNo)` (FDI quadrant 2/3 vs 1/4) so mesial always points toward the arch midline on both halves of the arch. Purely presentational — no `odontogram.ts` derivation/serialization change, no `perioClassification.ts` change, no `fhir/*` change, and the perio chart is not part of the golden fixtures — SVG-fingerprint, FHIR-golden, and roundtrip-golden fixtures are byte-identical (v1.48.0); payload stays 2.18.
- **Periodontal export: standalone SVG/PNG/JPG/PDF + case identity (UI-3b):** `caseMeta` gains two identity-only fields — `patientName` (trimmed string or `null`) and `examDate` (`YYYY-MM-DD` or `null`), via `setPatientName`/`setExamDate` — feeding the PDF report header only; NOT part of the FHIR export and not fed into the periodontal derivation. Payload **2.18 → 2.19** (additive). `hasAnyPerioData()` (`src/odontogram.ts`) is `true` iff any periodontal axis is charted anywhere in the mouth (derived from `getPerioSummary()` plus a direct scan for the three axes the summary doesn't surface — `cejVisibility`/`rootConcavity`/a non-deficient KG value); it drives the perio-export auto-skip and disables the perio export-menu items on a blank chart. The peri-implant Mombelli indices (mPI/mBI) are now gated **per arch**: a row renders only in an arch containing at least one implant tooth (`teeth.some(isToothImplant)`), on both the live perio chart and the new exports — mirrored, not duplicated, between `PerioChart.tsx` and `buildPerioSvg()`. `buildPerioSvg()` (new `src/perioExport.ts`) renders the FULL perio chart — tooth graphics (reusing `perioGraphic.ts`'s `buildBuccalArchSvg`/`buildPalatalArchSvg`/`archToothLayout`), every numeric row, and the 2017 classification block — as ONE standalone vector `<svg>`, built headlessly from state (never from the mounted `PerioChart` DOM); row order/value sources/gating mirror `PerioChart.tsx`'s `buildArch` exactly (`getPerioRowVisibility()` per row, `indexName()` for labels). `exportPerioSvg()`/`exportPerioImage("png"|"jpg")` consume it, wired to three new export-menu items, each disabled when `hasAnyPerioData()` is false. `exportPdf(opts)` (new `src/perioPdf.ts`) assembles a PDF report **jsPDF-natively — vector text via `.text()`, raster tooth/perio-chart images via `.addImage()` — with NO svg2pdf.js dependency** (jsPDF was already a dependency; this is its first use); `assemblePdf(opts, data, docFactory)` is split out PURE (no I/O/rasterization) for testability, driven by an injectable `PdfDocLike` (a real `jsPDF` can't be constructed in jsdom). Sections are independently optional (`{patientData, odontogram, perioStatus, perioDescription}`); the two perio sections auto-skip whenever `hasAnyPerioData()` is false regardless of the requested options. A `rasterizeSvgToPng`/`rasterizeSvgToCanvas` helper pair, extracted out of `exportImage`'s previously-duplicated raster logic, is shared by `exportImage`, `exportPerioImage`, and `exportPdf` (DRY). `ExportOptionsModal` (new `src/ExportOptionsModal.tsx`) is the "PDF report…" export-menu item's settings dialog — mirrors `DualStateConfirm`'s focus-trapped dialog contract exactly; four checkboxes (all default ON: patient data, odontogram, perio status, perio description — the two perio checkboxes are `disabled` with a "no perio data" hint when `hasAnyPerioData()` is false, and forced off in the `exportPdf()` call regardless of checked state, belt-and-suspenders on top of `exportPdf()`'s own auto-skip) plus patient-name/exam-date inputs wired straight to `setPatientName`/`setExamDate`. Disabled export-menu items get a dedicated greyed-out CSS style. Neither `patientName`/`examDate` nor any export path has an `svgLayer` on the odontogram itself → SVG-fingerprint and FHIR-golden parity fixtures are byte-identical to v1.48.0; the roundtrip-golden fixture changes only by its version string (v1.49.0).
- **Furcation + plaque (perio axes):** `furcation` per tooth = `Map<entrance, grade 1–4>` (Glickman I–IV); `furcationEntrances(toothNo)` is the single source (position-based: upper molars→[mesial,distal,buccal], lower molars→[buccal,lingual], upper 1st premolars 14/24→[mesial,distal], else []); render/authoring additionally gates on a present-natural tooth. `plaque` per tooth = `Set<surface>` over the 4 O'Leary surfaces; whole-mouth `getPerioSummary().plaquePercent` = plaque surfaces ÷ (present-teeth × 4), present = `isToothPresent` (excludes missing/implant). Both omit-when-empty; payload **2.13** (additive). API: `setFurcation`/`getToothFurcation`, `setPlaque`/`getToothPlaque`; summary gains `maxFurcation` + `plaquePercent`. FHIR (`toFhirPerio.ts`): furcation → LOINC `34015-8` per entrance; plaque → per-surface boolean components with a local `plaque-surface` code (no verified O'Leary LOINC, like per-site BOP); both ride the R4 `component.bodySite` backport extension. Rendered as rows (not tooth-glyphs) in both the grid and the graphical Dental Chart (furcation = per-entrance none→I→IV cycle; plaque = 4-surface toggle). Furcation/plaque participate in the R2-B `getPlanChanges()` diff via two SUMMARY-level `DIFF_AXES` entries (`furcation`: the tooth's highest Glickman grade via the same `furcation.grade.N` copy the picker uses; `plaque`: a compact `N/4` surface count) — never per-entrance/per-surface explosion, mirroring the existing `perio` axis. KEYBOARD: probing depth 10–15 via a leading-`1` two-keystroke rule (`dataset.pendingTens`, cleared on blur); `setReadOnly` also locks the perio overlay/inline container, and calls `notifyStateChange()` (bead odontogram-v90) so a flip taken while the chart is mounted re-runs `PerioChart`'s `fullResync()` — every control's `disabled` attribute is re-derived from `getReadOnly()` plus its own gates (`perioAxisApplies`, charted state), instead of keeping the value it was built with while only the `.read-only` CSS class changed. `setReadOnly` is therefore no longer the one app-level session flag that does not notify.
- **Perio-chart overlay (`src/PerioChart.tsx`):** the full-mouth periodontal grid is a **separately-invocable overlay** — a `PerioChart` React component (`{ open, onClose }`, mirroring `SettingsModal`'s dialog contract) exported as a NAMED export from `App.tsx` (alongside the `OdontogramShell` default export), plus `openPerioOverlay()`/`closePerioOverlay()`/`isPerioOverlayOpen()` in `odontogram.ts` (a module-level flag + `notifyStateChange`). A host app can mount `PerioChart` or call the open/close API to surface the perio chart independently of the base odontogram; it shares the same module-level case state via the P1 API (one case, two surfaces) and is dual-state aware. The grid = two arch bands (18→11,21→28 / 48→41,31→38), teeth × 6 sites, buccal/lingual PD·GM·CAL(read-only)·BOP rows + mobility + a summary bar (%BOP/worst CAL/max PD); all writes go through `setPerioSite` (no new mutation/derivation). **Keyboard auto-advance:** a pure `nextPerioCell(toothNo,site,row)` charting order (MB→B→DB per tooth across the arch, wrap to next row); a digit commits+advances, arrows navigate, space toggles BOP, a leading `-` primes a negative GM (cleared on blur), an emptied PD un-charts. The overlay is a sibling of the odontogram which is NEVER unmounted → SVG-fingerprint parity byte-identical. `<PerioChart>` makes no perio module calls while closed (mount is `open`-gated). (PD 10–15 keyboard entry and live readOnly-locking of the open overlay were added in later releases.)
- **FHIR:** a bespoke per-site builder (`src/fhir/toFhirPerio.ts`, called from `toFhir.ts` — NOT via the declarative `fieldMappings.ts` axis path) emits one periodontal-panel `Observation` (LOINC `74029-0`) per charted tooth, with per-site `component[]` PD `32910-2` / recession `32911-0` (gm>0 only) / CAL `32912-8` (derived) / BOP (boolean, no per-site LOINC). The tooth+probe-site qualifier rides on `component.extension` via HL7's R4 backport URL `http://hl7.org/fhir/5.0/StructureDefinition/extension-Observation.component.bodySite` (R5's `component.bodySite` is illegal on an R4 bundle). SNOMED IDs NOT hardcoded (maintainer-verify against the SNOMED Odontogram refset); FHIR *import* of perio deferred (round-trips via JSON payload only).

- **`isImplantProductGap(toothNo)`** (odontogram-im1, the piece that waited on ap7): an empty `implantProduct` is a GAP only on an implant this practice placed. "We placed it, nothing recorded" = incomplete; "it came with the patient" = complete (Dirk: not every patient carries an implant passport). Derived — never a second provenance flag inside im1, since ap7 owns that question for every finding. Returns `false` whenever `getBaselineExamination()` is `null`: with no archive there is no baseline to judge against, and warning then would be a guess. Surfaced as `#implantProductGap` inside the implant block.

## Choosable restoration colours (odontogram-sjr)
- Every restoration fill in the GENERATED assets is `fill: var(--odon-rest-<key>, #hex)` (and each of the two ramp materials' nine `<stop>`s likewise). Choosing a colour = setting a custom property on the component root; the cascade repaints and **no JavaScript runs in the render path**. Unconfigured = byte-identical, because nothing is written and every fallback is the shipped colour. `applyRestorationPalette` REMOVES an unchosen entry's properties instead of writing the default back — writing it back leaves a chart that merely looks unconfigured while carrying inline properties that outlive a reset.
- Authored in `tools/toothgen/source/*.svg` and regenerated. **`npm run toothgen:build` does NOT rebuild the occlusal templates** — they have their own script, `uv run tools/toothgen/occlusal.py`. Missing that makes a source edit look like it silently failed on the twenty `_occl` assets.
- Keyed on LAYER ID, never on the hex: that is what keeps 58n's two reported collisions separable (GIC shares `#f9ae94` with the denture base; a metal crown shares `#0051bf` with a telescopic one). Same defaults, separate variables.
- `src/restorationPalette.ts` is DOM-free apart from `applyRestorationPalette`, so the colour maths is testable without a browser. `deriveRamp()` re-hues a nine-stop ceramic ramp while KEEPING its lightness sweep (each stop takes the picked hue, keeps its own L, and keeps its S in proportion to the ramp's most saturated end) — flattening the ramp would cost exactly what makes it read as ceramic. `resolveEntry` also derives the lighter ANTERIOR telescope connector from the posterior tone, so one picker drives both and a deliberate contrast is not unified away. A ramp entry's picker shows the ramp's most saturated END, not its near-white first stop.
- **Session state, never payload** (Dirk): a practice preference, not patient data, so it sits with `perioViewMode` and friends. Accepted consequence: a case exported here and opened elsewhere renders in THAT practice's colours. `getRestorationPalette`/`setRestorationPalette` let a host persist it; `setRestorationColour(key, hex|null)` and `resetRestorationColours()` are the interactive API. Settings → **Colours** (appended LAST in `SETTINGS_TABS`; two tests pin the general → panels → toothDetails run).
- `fillLightness()` in `odontogram.ts` parses a `var(--x, #hex)` FALLBACK, because odontogram-ap7's pre-existing hatch reads `style.fill` to paint the finding's own colour under the hatch lines and every restoration fill is now a `var()`. The fallback is the only value a script here can know — the cascade owns the rest.
- Drifts unified while measuring: amalgam `#a0a0a0` (4 occlusal) vs `#aaa`; the same split on `telescope-crown-inside`; `#fffffb` vs `#fff` on the temporary bridge connector; three near-identical metal-ceramic ramps.

## Retention elements (odontogram-dma)
- **Gating is three anchorings, not one rule** (`src/retention.ts`, DOM-free like `bridgeOverlay.ts`): `retentionOptions(state)` offers `clasp` on any present NATURAL tooth (crowned or not) and adds `attachment`/`bar-abutment` only when `restorationType` is `crown` or `bridge`. An implant returns `["none"]` — it answers the same question on the existing `prosthesis` axis (`locator`/`bar`/`bar-denture`), and asking twice in two places is how two answers drift. `retentionAllowed()` is the SINGLE predicate shared by the picker, `setRetention` (guard BEFORE the DS-1 gate), the tooltip and the summary, so a tolerantly-imported attachment on an un-crowned tooth is stored but invisible everywhere the control refuses it. `setRetention(tooth, "none")` clears the side with it.
- **The telescope is retention without a second place to store it:** `isTelescopeRetention()` reads `restorationMaterial === "telescope"` on a crowned tooth — tooth-kind agnostic, so a telescope on an IMPLANT counts too (Dirk: telescopes on implants and natural teeth occur mixed). It stays a crown material, which is what it is and what already draws as a double contour; this keeps the exclusion rule true without a special case (a telescope abutment carries no clasp because its retention IS the crown).
- **The bar span is DERIVED** (`detectBarSpans`), like a bridge span but NOT the same derivation: a bridge is continuous, so `detectBridgeSpans` runs over ADJACENT teeth; a bar crosses the edentulous space it exists to span, so abutments are taken in arch order and connected first-to-last whatever lies between. Requiring adjacency would find nothing on a real case. `isBarAbutment()` counts BOTH routes — `retention === "bar-abutment"` on a natural tooth, `prosthesis` `bar`/`bar-denture` on an implant — so one bar resting on mixed abutments draws as one element. Same documented limitation as `detectBridgeSpans`: two bars in one arch merge, since state carries no per-bar grouping id.
- **Render lives in the GRID overlay, never in a tooth's SVG** → SVG-fingerprint parity byte-identical. `updateRetentionBarOverlay()` (called from `updateBridgeOverlay`) draws into its own `svg.retention-overlay` using the SAME `tileRectFor` geometry the bridge saddle reads, so the two can never disagree about where a tooth is. `computeRetentionBars` draws ONE run per span (gap-by-gap would break at every tile boundary — the failure `detectBridgeSpans` exists to prevent) at `BAR_Y_FRACTION` 0.62, above the crown at the gingival margin. `computeClaspGlyphs`/`claspPath` draw a QUARTER arc whose centre sits at the crown's occlusal-inward corner, so one end lands at the height of contour, the more occlusal end at the interdental space, and the belly faces the gingiva — all three from one construction. Two directions drive it from DIFFERENT sources: inward is the engaged SIDE (`mesialIsLeft`, quadrant 2/3), occlusal is the ARCH (lower draws crowns up), and the arc's rotational sense is derived from their product rather than tabulated. `CLASP_EDGE_INSET` anchors the proximal end inside the tile edge, since a tile is wider than the crown it holds.
- `retentionMark()` is charly's own shorthand, read off its keypad rather than paraphrased: `<Kl`/`Kl>` (angle brackets), `( G`/`G )` (ROUND brackets — the bracket shape is vocabulary, not decoration), and `ste` for the bar with no bracket at all (a bar does not engage one side of one tooth). Only the attachment and the bar use it as a tile badge; the clasp is drawn.

## Provenance: what the patient arrived with (odontogram-ap7)
- **DERIVED, never stored.** The dated examination archive (odontogram-2vd) already holds the mouth as the patient brought it, so "pre-existing" is a comparison against the EARLIEST archived examination — no payload bump, no FHIR mapping, and no second record that could drift out of agreement with the archive. It compares `charts.status` EXPLICITLY, never the active-chart alias: an examination records what was observed, and provenance is a fact about the past a proposal cannot change. API: `getBaselineExamination()`, `getPreExistingAxes(toothNo)`, `getChangesSinceBaseline()` (same `PlanChange[]` shape/order as `getPlanChanges`), `isToothPreExisting(toothNo)` (the question `odontogram-im1` consumes — an implant present at the baseline arrived with the patient, so an empty implant product on it is a complete record rather than a gap).
- **`PROVENANCE_AXES` = `DIFF_AXES` + `fillings` + `caries`.** `DIFF_AXES` is deliberately NOT extended: it answers "what does the plan change", where a filling and a caries lesion are not treatment-plan axes, and growing it would silently change the "What changes" box. An axis only counts as a finding when its label differs from what a HEALTHY `defaultState()` tooth reads (`axisCarriesFinding`) — a `planChange.none` sentinel test alone reports every healthy tooth as carrying two findings, since `presence` reads "Tooth" and `substrate` reads "natural".
- **Render (`applyPreExistingStyling`, run in `applyStateToSvg`'s roots loop AFTER `applyProposedStyling`):** hatches the layers active NOW that were also active in the baseline, minus the anatomy a healthy tooth draws (`healthyLayerIds`, cached per template+tooth). Marking goes through `style.fill` → a `<pattern>` in `<defs>` whose background is the shape's OWN colour (`fillLightness` parses BOTH `#rgb` and `rgb(...)` — `style.fill` reads back normalized, so hex-only would drop the colour on every real layer) and whose line colour flips dark/light by luminance. `data-base-pfill` capture-once + symmetric `clearPreExistingStyling`, mirroring the proposed-styling and SP12 discoloration pattern. Gated on `chartMode === "status"` (Plan mode leaves the tooth to the dashed channel) AND an archive existing — the parity fixtures archive none, so the capture never runs it; `style.fill` is in neither the SVG fingerprint (id/opacity/class) nor `tools/toothgen/verify.py`'s frozen geometry digests, so hatching is parity-safe on both contracts.
- **The hatch marks WORK, never the tooth and never the disease** (`isRestorativeLayer`): composed restorations (`allRestorationLayers()` + the per-material wrapper `<g>`s), direct fillings (`filling-` prefix), endodontic fillings/pins, apicoectomy, fissure sealing. A radix or an implant is a tooth, not work; caries/calculus/periodontal findings are disease. `isActiveWithin` skips anything switched off between the shape and its layer, so the wrapper `<g id="gold">` no longer paints its inactive `gold-inlay`/`gold-veneer`/`gold-bridge-connector` siblings. The DERIVATION stays broad — the chart narrows, the record does not.
- **`repaintForBaselineChange()`** runs on `captureExamination`/`resetExaminations`/`removeExamination`: a new baseline changes what EVERY tooth means, exactly as a chart-mode switch does. Without it hatching appeared only on teeth edited afterwards. No-ops when no grid is mounted (`toothSvgRoot.size === 0`).
- **Correcting the initial examination** (Dirk's choice over a per-tooth override — one account of what the patient arrived with, not a second note beside it): `beginBaselineCorrection()` / `commitBaselineCorrection()` / `cancelBaselineCorrection()` / `isCorrectingBaseline()`. The round trip lives in `odontogram.ts`, NOT the UI, because `loadExamination` replaces the live case — entering without stashing today's findings would DESTROY them. `commitBaselineCorrection` passes the examination's OWN id, which `captureExamination` replaces in place, so it keeps its identity and date. The stash is runtime-only and never serialized, so the UI must make the mode unmissable (`#baselineCorrectionBanner`).
- **Import (`importAsBaseline`, session flag, default ON, never serialized):** a chart arriving by import carries no archive, yet everything in it is pre-existing from this practice's view — the normal way a foreign chart enters. `adoptImportAsBaseline(broughtItsOwnArchive)` archives it as the initial examination dated today, and is called from BOTH `importStatus` and `__importStatusForTest` but deliberately NOT from `hydrateImportedCharts` (every load goes through that, including `loadExamination` and the correction round trip, where archiving would file an examination mid-correction). `broughtItsOwnArchive` is read BEFORE hydrating, since hydration replaces `examinations`. A document that brought its own archive keeps it, so a re-import is never re-dated over the patient's real intake date. Surfaced as a checkbox in the import menu (the file picker is the OS's, so the menu is the only place to ask). `getImportAsBaseline`/`setImportAsBaseline`.
- UI: `src/ExaminationCard.tsx` (`#examinationCard`, mounted at the top of the control panel) — capture with a date, the baseline named, the archive list, and the correction round trip. It subscribes to `onStateChange` itself, like `PerioChart`/`PerioSidebar`.

## Orthodontics (bead odontogram-c51 and children)
- **Model analysis (c51.1)** — `src/modelAnalysis.ts`, dependency-free and pure like `perioClassification.ts`. `deriveModelAnalysis({widths, absentTeeth})` returns the six sums, Tonn / Bolton-anterior / Bolton-overall as `RatioIndex` (actual, target, delta, which arch carries the excess and by how many mm), the Tonn target incisor sums, and `substitutions`. Norm constants carry their publication beside them (Tonn 1937 → 74,0 %; Bolton 1958 → 77,2 % / 91,3 %). **A sum whose teeth are not ALL measured is `null`, never a partial total.** The millimetre figure is the CLASSICAL Bolton excess, deliberately not the reference printout's `delta% × numerator` — both are documented and a test pins the divergence.
- **Contralateral substitution** — a tooth that is not on the model borrows its partner's width (`contralateral()` mirrors FDI quadrants 1↔2, 4↔3, 5↔6, 8↔7). Driven by `isToothMeasurable(toothNo)` in `odontogram.ts`, which is FALSE for `none` / `not-erupted` / `no-tooth-after-extraction` / `tooth-under-gum`, and for an implant UNLESS it carries a crown or bridge (Dirk's ruling; he notes it has not arisen in thirty years). An absent tooth NEVER uses a width stored against itself (ignored, not deleted); substitution never chains. Every borrowed width surfaces in `substitutions` and renders read-only, tinted and listed — an inference must never read as a measurement.
- **Cephalometry (c51.2)** — `src/cephalometry.ts`, three layers: `LANDMARKS` (anatomic / radiographic / constructed), `MEASURES` over them, and `PROFILES` selecting from those. **Norms belong to the PROFILE** (`CephProfile.norms`, resolved by `normFor(measureId, profileId)`), because schools disagree — ML-NSL is 28° in Hasund and 32° in Segner, and the same value reads vertical against one and neutral against the other. A profile declares its own `referenceFrame`, including `"none"` so Sato's Denture Frame Analysis stays representable.
- **SOURCING RULE, enforced by a test** — every measure carries a mandatory `source`, and a norm without a publication is NOT shipped (`norm`/`sd` stay `null` and the UI shows no target). Norms come from Paddenberg et al. 2021 and from Segner & Hasund 1998 as tabulated by the Regensburg clinic; classification rules and both clinical equations come first-hand from Habersack & Hasund's own manual. Two conflicts are left VISIBLE rather than resolved (Paddenberg gives ML-NSL ± 2,0 against Segner's ± 6,0; the two also differ on SNA/SNB means).
- **FHIR coding lives on the measure** (`coding: { local, ucum, loinc?, snomed? }`), not in a later mapping table — the same inline pattern `ClinicalAxis.finding` / `AxisValue.coding` already uses, so there is no second layer to drift. No LOINC or SNOMED code is invented; none verified for a cephalometric measurement could be produced, mirroring `toFhirPerio.ts`'s treatment of per-site BOP and the Mombelli indices.
- **Derivations** — `deriveJawRelation` (facial type after Björk with Hasund's limits, harmony, and the sagittal class reported TWICE: against the individual norm and against the population one, because their disagreement IS the finding); `deriveGrowthPattern` (a vote across every indicator with a sourced norm, each reporting its deviation, its own reading and whether a `published-band` or the `standard-deviation` fallback decided it, plus a `dissent` count; an empty form is `indeterminate`, not `neutral`); `individualisedAnb` / `individualisedAnbExtended` / `individualisedWits` (Paddenberg 2021 — deliberately NOT the classical coefficients the practice software runs, whose publication has not been read).
- **Import (`src/cephImport.ts`)** — `parseCephText(text)` takes TEXT, never a PDF: pasting works with no dependency and a PDF reader would feed the same parser. Returns per-line `confidence` (`high` only when the full norm/measurement/deviation triple is present, so the SECOND number is unambiguously the measurement) plus every `candidate` number. **Nothing is applied without confirmation** — a printed sheet has rows carrying only a norm, and an importer grabbing the first number would file a norm as a finding. The number regex uses a lookbehind because cephalometric labels are full of digits (`OK1-UK1`).
- **Persistence** — tooth widths (`getToothWidths`/`setToothWidth`), the four occlusal readings (`getOcclusalMeasurements`/`setOcclusalMeasurement`), cephalometric values (`getCephValues`/`setCephValue`) and the active profile are SESSION state, deliberately outside the export payload: c51's audit found no published Dental Core carrier, and c51's rule is to stay blocked rather than invent a local-code path. Payload version, FHIR bundle and SVG fingerprints are unchanged, and tests pin that boundary. The promotion point is marked in `odontogram.ts`.
- **UI** — `ModelAnalysisCard.tsx` (arch + list, switchable, one record) and `CephalometryCard.tsx` (profile selector, live norms, paste importer, jaw-relation and growth panels). Neither is wired into `App.tsx`: where they live is c51's decision. `npm run prototype:model` opens a bench. Both use `type="text"` with `inputMode="decimal"`, NOT `type="number"` — a number input silently discards the decimal comma every German-speaking user types.

## Chart State Model (Status vs Plan)
- **Status→Plan edit propagation (DS-1):** a per-tooth runtime `Set planEditedTeeth` marks teeth explicitly edited while in Plan mode. ALL interactive tooth edits route through a central gate — `gateToothEdit(toothNo, applyFn)` (single) / `gateToothEditBatch(toothNos, applyFn)` (atomic over a set; `applyStatusExtra`/`applyToSelected`/`setEdentulous`/dentition presets use it). Logic: **plan mode** → `applyFn()` + mark the tooth; **status mode, tooth NOT plan-edited** (or plan uninitialized) → `applyFn()` + `mirrorStatusToPlan(toothNo)` (deep copy via serialize→hydrate) so the plan tracks the corrected reality and the tooth shows NO diff; **status mode, tooth IS plan-edited** → a **blocking in-app confirm** (`src/DualStateConfirm.tsx`, mirrors SettingsModal) BEFORE applying — accept applies to status only (plan stays → they diverge, the diff shows it), cancel applies nothing + reverts the control. Batches confirm ONCE over the whole set and apply all-or-none. `applyFn` returning `false` (no-op) skips marking/mirroring. `planEditedTeeth` is runtime-only (NOT serialized) and cleared on plan (re)init / reset / import / `setPlanChart`. NON-interactive paths (import/hydrate, reset/clear buttons, `cloneChart`, lazy `defaultState()` vivify) are NOT gated. Parity byte-identical (edit-routing only).
- Two parallel per-case states, `charts.status` and `charts.plan` (both `Map<toothNo, ToothState>`, declared as `const charts: Record<"status" | "plan", Map<...>>` in `src/odontogram.ts`). `toothState` is a module-scoped, REASSIGNABLE `let` alias pointing at whichever chart is currently active — every pre-existing `toothState.get/set/...` call site keeps operating on the active chart unchanged.
- `chartMode: "status" | "plan"` tracks the active chart (default `"status"`). `getChartMode()`/`setChartMode(mode)` are the public read/switch API; `setChartMode()` reassigns `toothState`, does a full repaint of every tooth (reusing the same repaint loop `importStatus()` uses — no new render path), and syncs the header toggle UI.
- The plan chart is lazily initialized: the FIRST time `setChartMode("plan")` is called, `charts.plan` is deep-copied from `charts.status` via the existing `serializeState → hydrateState` round-trip (`cloneChart()`), guaranteeing independent Maps/Sets so edits never leak between charts. Later switches reuse whatever is already in `charts.plan` — it is never re-cloned from status once initialized.
- UI: a `Status | Plan` segmented toggle (`#chartModeToggle`, buttons `#chartModeStatus`/`#chartModePlan`) sits in the dental-chart card header (`src/App.tsx`); the active chart also gets a `.plan-mode` class on the chart card as a visual cue when Plan is active.
- Per-state public API (independent of which chart is active): `getStatusChart()` / `getPlanChart()` return each chart's payload (`{version, globals, teeth}`); `setPlanChart(payload)` hydrates the plan chart only (status untouched), repainting the DOM only if plan is the currently active mode.
- Export/import stay status-primary: `exportStatus`/`exportFhir`/file import always target `charts.status` explicitly, never the active-chart alias — exporting while in Plan mode still yields the status chart. The JSON export gains an additive top-level `plan` field, emitted only when the plan chart is initialized AND differs from status; payload version bumped to **2.11** (imports still accept legacy 1.4–2.10). Import restores `plan` when present; its absence clears/uninitializes the plan chart (never resurrects a stale plan from before the import).
- FHIR export/import remain status-only — `buildFhirBundle()`/`parseFhirBundle()` are unaffected by the plan chart; there is no FHIR representation of the plan chart yet.
- Render is byte-identical to the pre-dual-state single chart (`parity.test.ts` SVG fingerprints) — the toggle only changes WHICH chart is drawn, never how a chart renders.
- **Proposed styling (plan-only render):** in Plan mode, any SVG layer active in the plan chart but NOT in the status chart for that tooth (a finding the plan *adds*) renders with a distinct dashed, tinted "proposed" outline. `applyProposedStyling(toothNo, svg)` runs in `applyStateToSvg`'s loop AFTER `applyStateToSvgSingle` (never inside it — it clones the svg and re-invokes `applyStateToSvgSingle` to render the status state, which would recurse): it computes `planIds ∖ statusIds` (status rendered into `svg.cloneNode(true)` via `applyStateToSvgSingle(toothNo, clone, charts.status.get(toothNo) ?? defaultState())`, then `collectActiveLayers`) and marks the plan-only layers via the **inline-style channel** (`style.strokeDasharray` + a `style.stroke` tint) — never `style.opacity` or a CSS class, the two channels `collectActiveLayers` fingerprints. Gated on `chartMode === "plan" && planInitialized`; `clearProposedStyling` runs first on every call and restores each captured `data-base-dash`/`data-base-pstroke` (symmetric reset, so switching back to Status or editing a plan finding to match status leaves no stray dashes). A `#proposedLegend` ("dashed = proposed") shows via pure CSS under the chart card's `.plan-mode` class. Status-mode render + parity capture never run this branch → byte-identical.
- **Status→Plan diff:** `getPlanChanges()` returns a pure, read-only diff of the two charts — `PlanChange[]` (`{ toothNo, axis, from, to }`), one entry per tooth per treatment axis whose human-readable label differs between `charts.status` and `charts.plan`; `[]` when the plan is uninitialized. It compares over `DIFF_AXES` (presence, substrate, restoration, prosthesis, crownAction, ortho, pulpEndo, apical), each described by the SAME label helper the tooltip/summary already use (no reinvented copy). The list is also exposed on `getOdontogramSummary()` as `plannedChanges`; `App.tsx` renders it as a distinct "What changes" box (`#plannedChangesBox`) under the Tooth-information panel, shown only when non-empty. No render/state/payload change (parity byte-identical).

## Integration with DentalQuoteCreator
- Used as git submodule at: `src/modules/odontogram/engine`
- Host app imports via path alias: `@odontogram-shell` → `src/App.tsx`
- Type declarations: `src/modules/odontogram/odontogram-shell.d.ts` in the host project
- Theme is synced via `themeConfig` prop (CSS variables)

## Build & Check
- `npm run dev` — start Vite dev server
- `npm run build` — production build (`tsc -b && vite build`)
- `npm test` — run Vitest tests
- `npm run test:coverage` — coverage report
- `npx tsc -b --noEmit` — type check only

## Conventions
- Tooth IDs use FDI notation (11-48) as internal keys
- All user-facing strings go through `src/i18n/translations.ts`
- SVG layers follow naming: `{toothId}_{surface}.svg`
- State is serialized as JSON for storage/transfer
- Read-only mode is supported via `readOnly` prop
- Keyboard accessibility (WCAG) is implemented for tooth navigation

## Documentation (mandatory)
- Updating the documentation is **mandatory** for every user-facing change (new
  feature, changed behavior, new/renamed public API, new state field or enum value).
- Keep `CHANGELOG.md` current — add an entry (Keep a Changelog format) and bump the
  version in `package.json` and the README version badge for each release.
- **English is the source of truth** for the README; translate the others from it.
- Maintain a README for **every UI language the program supports** (currently
  HU, EN, DE, ES, IT, SK, PL, RU, PT-BR, AR, ZH, FR — see `Language` in
  `src/i18n/translations.ts`). `README.md` is the English top level and links all twelve; the per-language guides live
  in `lang/README-<code>.md` (e.g. `lang/README-de.md`, `lang/README-pl.md`,
  `lang/README-ru.md`, `lang/README-ar.md`, `lang/README-zh.md`). When a new UI
  language is added, add its README too and update every language switcher.
- **RTL:** Arabic (`ar`) is the only RTL language. The document root `dir`
  attribute reacts to the active language (`rtl` for `ar`, `ltr` otherwise), so
  the whole UI mirrors; the dental chart (`#toothGrid`) and the periodontal
  charts are explicitly pinned `dir="ltr"` (attribute + CSS) so tooth
  geometry/numbering never flips.

## Scope Control
- Do not add dependencies on external dental systems or APIs
- Do not add routing — this is a single component, not an app
- Keep the bundle size minimal — no heavy libraries
- SVG assets should be optimized (no unnecessary metadata)
