// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { CodeableConcept } from "../../hl7-fhir-r4-core/CodeableConcept";
import type { Procedure } from "../../hl7-fhir-r4-core/Procedure";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";

import {
    ensureProfile,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateSliceFields,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateChoiceProhibited,
    validateMustSupport,
} from "../../profile-helpers";

export type DentalProcedureProfileRaw = {
    status: ("preparation" | "in-progress" | "not-done" | "on-hold" | "stopped" | "completed" | "entered-in-error" | "unknown");
    code: CodeableConcept;
    subject: Reference<"Group" | "Patient">;
    bodySite: CodeableConcept[];
}

// CanonicalURL: https://fhir.cognovis.de/dental-core/StructureDefinition/dental-procedure (pkg: de.cognovis.fhir.dental.core#0.5.0)
export class DentalProcedureProfile {
    static readonly canonicalUrl = "https://fhir.cognovis.de/dental-core/StructureDefinition/dental-procedure";

    private resource: Procedure;

    constructor (resource: Procedure) {
        this.resource = resource;
    }

    static from (resource: Procedure) : DentalProcedureProfile {
        if (!resource.meta?.profile?.includes(DentalProcedureProfile.canonicalUrl)) {
            throw new Error(`DentalProcedureProfile: meta.profile must include ${DentalProcedureProfile.canonicalUrl}`)
        }
        const profile = new DentalProcedureProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static is (resource: unknown) : resource is Procedure {
        if (typeof resource !== "object" || resource === null) return false;
        const r = resource as { resourceType?: string; meta?: { profile?: string[] } };
        if (r.resourceType !== "Procedure") return false;
        return (r.meta?.profile ?? []).includes(DentalProcedureProfile.canonicalUrl);
    }

    static apply (resource: Procedure) : DentalProcedureProfile {
        ensureProfile(resource, DentalProcedureProfile.canonicalUrl);
        return new DentalProcedureProfile(resource);
    }

    static createResource (args: DentalProcedureProfileRaw) : Procedure {
        const resource: Procedure = {
            resourceType: "Procedure",
            status: args.status,
            code: args.code,
            subject: args.subject,
            bodySite: args.bodySite,
            meta: { profile: [DentalProcedureProfile.canonicalUrl] },
        }
        return resource;
    }

    static create (args: DentalProcedureProfileRaw) : DentalProcedureProfile {
        const resource = DentalProcedureProfile.createResource(args);
        return DentalProcedureProfile.apply(resource);
    }

    toResource () : Procedure {
        return this.resource;
    }

    // Field accessors
    getStatus () : ("preparation" | "in-progress" | "not-done" | "on-hold" | "stopped" | "completed" | "entered-in-error" | "unknown") | undefined {
        return this.resource.status as ("preparation" | "in-progress" | "not-done" | "on-hold" | "stopped" | "completed" | "entered-in-error" | "unknown") | undefined;
    }

    setStatus (value: ("preparation" | "in-progress" | "not-done" | "on-hold" | "stopped" | "completed" | "entered-in-error" | "unknown")) : this {
        Object.assign(this.resource, { status: value });
        return this;
    }

    getCode () : CodeableConcept | undefined {
        return this.resource.code as CodeableConcept | undefined;
    }

    setCode (value: CodeableConcept) : this {
        Object.assign(this.resource, { code: value });
        return this;
    }

    getSubject () : Reference<"Group" | "Patient"> | undefined {
        return this.resource.subject as Reference<"Group" | "Patient"> | undefined;
    }

    setSubject (value: Reference<"Group" | "Patient">) : this {
        Object.assign(this.resource, { subject: value });
        return this;
    }

    getBodySite () : CodeableConcept[] | undefined {
        return this.resource.bodySite as CodeableConcept[] | undefined;
    }

    setBodySite (value: CodeableConcept[]) : this {
        Object.assign(this.resource, { bodySite: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "DentalProcedure"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "status"),
                ...validateEnum(res, profileName, "status", ["preparation","in-progress","not-done","on-hold","stopped","completed","entered-in-error","unknown"]),
                ...validateRequired(res, profileName, "code"),
                ...validateRequired(res, profileName, "subject"),
                ...validateReference(res, profileName, "subject", ["Group","Patient"]),
                ...validateRequired(res, profileName, "bodySite"),
            ],
            warnings: [],
        }
    }

}
