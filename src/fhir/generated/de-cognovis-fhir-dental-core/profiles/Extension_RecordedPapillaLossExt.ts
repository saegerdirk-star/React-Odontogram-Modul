// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Coding } from "../../hl7-fhir-r4-core/Coding.js";
import type { Extension } from "../../hl7-fhir-r4-core/Extension.js";

import {
    isRawExtensionInput,
    applyFixedValue,
    isExtension,
    getExtensionValue,
    pushExtension,
    upsertExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateSliceFields,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateChoiceProhibited,
    validateMustSupport,
} from "../../profile-helpers.js";

export type RecordedPapillaLossExtProfileRaw = {
    extension?: Extension[];
}

export type RecordedPapillaLossExtProfileFlat = {
    surface: Coding;
    grade: number;
    classification: string;
}

// CanonicalURL: https://fhir.cognovis.de/dental-core/StructureDefinition/recorded-papilla-loss (pkg: de.cognovis.fhir.dental.core#0.7.1)
export class RecordedPapillaLossExtProfile {
    static readonly canonicalUrl = "https://fhir.cognovis.de/dental-core/StructureDefinition/recorded-papilla-loss";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : RecordedPapillaLossExtProfile {
        const profile = new RecordedPapillaLossExtProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static is (resource: unknown) : resource is Extension {
        if (typeof resource !== "object" || resource === null) return false;
        return (resource as { url?: string }).url === RecordedPapillaLossExtProfile.canonicalUrl;
    }

    static apply (resource: Extension) : RecordedPapillaLossExtProfile {
        resource.url = RecordedPapillaLossExtProfile.canonicalUrl;
        applyFixedValue(resource, "url", "https://fhir.cognovis.de/dental-core/StructureDefinition/recorded-papilla-loss");
        return new RecordedPapillaLossExtProfile(resource);
    }

    private static resolveInput (args: RecordedPapillaLossExtProfileRaw | RecordedPapillaLossExtProfileFlat) : Extension[] {
        if (isRawExtensionInput<RecordedPapillaLossExtProfileRaw>(args)) {
            return args.extension ?? [];
        } else {
            const result: Extension[] = [];
            if (args.surface !== undefined) {
                result.push({ url: "surface", valueCoding: args.surface } as Extension);
            }
            if (args.grade !== undefined) {
                result.push({ url: "grade", valueInteger: args.grade } as Extension);
            }
            if (args.classification !== undefined) {
                result.push({ url: "classification", valueString: args.classification } as Extension);
            }
            return result;
        }
    }

    static createResource (args?: RecordedPapillaLossExtProfileRaw | RecordedPapillaLossExtProfileFlat) : Extension {
        const resolvedExtensions = RecordedPapillaLossExtProfile.resolveInput(args ?? {});

        const resource: Extension = {
            url: "https://fhir.cognovis.de/dental-core/StructureDefinition/recorded-papilla-loss",
            extension: resolvedExtensions,
        }
        return resource;
    }

    static create (args?: RecordedPapillaLossExtProfileRaw | RecordedPapillaLossExtProfileFlat) : RecordedPapillaLossExtProfile {
        return RecordedPapillaLossExtProfile.apply(RecordedPapillaLossExtProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    // Extensions
    public setSurface (value: Coding): this {
        upsertExtension(this.resource, { url: "surface", valueCoding: value } as Extension)
        return this
    }

    public getSurface(mode: 'flat'): Coding | undefined;
    public getSurface(mode: 'raw'): Extension | undefined;
    public getSurface(): Coding | undefined;
    public getSurface (mode: 'flat' | 'raw' = 'flat'): Coding | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "surface")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<Coding>(ext, "valueCoding")
    }

    public setGrade (value: number): this {
        upsertExtension(this.resource, { url: "grade", valueInteger: value } as Extension)
        return this
    }

    public getGrade(mode: 'flat'): number | undefined;
    public getGrade(mode: 'raw'): Extension | undefined;
    public getGrade(): number | undefined;
    public getGrade (mode: 'flat' | 'raw' = 'flat'): number | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "grade")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<number>(ext, "valueInteger")
    }

    public setClassification (value: string): this {
        upsertExtension(this.resource, { url: "classification", valueString: value } as Extension)
        return this
    }

    public getClassification(mode: 'flat'): string | undefined;
    public getClassification(mode: 'raw'): Extension | undefined;
    public getClassification(): string | undefined;
    public getClassification (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "classification")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueString")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "RecordedPapillaLossExt"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", RecordedPapillaLossExtProfile.canonicalUrl),
            ],
            warnings: [],
        }
    }

}
