// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension.js";

import {
    isRawExtensionInput,
    applyFixedValue,
    isExtension,
    getExtensionValue,
    pushExtension,
    upsertExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateSliceFields,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateChoiceProhibited,
    validateMustSupport,
} from "../../profile-helpers.js";

export type RecordedApicalFindingExtProfileRaw = {
    extension: Extension[];
}

export type RecordedApicalFindingExtProfileFlat = {
    description: string;
    root?: string;
}

// CanonicalURL: https://fhir.cognovis.de/dental-core/StructureDefinition/recorded-apical-finding (pkg: de.cognovis.fhir.dental.core#0.6.1)
export class RecordedApicalFindingExtProfile {
    static readonly canonicalUrl = "https://fhir.cognovis.de/dental-core/StructureDefinition/recorded-apical-finding";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : RecordedApicalFindingExtProfile {
        const profile = new RecordedApicalFindingExtProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static is (resource: unknown) : resource is Extension {
        if (typeof resource !== "object" || resource === null) return false;
        return (resource as { url?: string }).url === RecordedApicalFindingExtProfile.canonicalUrl;
    }

    static apply (resource: Extension) : RecordedApicalFindingExtProfile {
        resource.url = RecordedApicalFindingExtProfile.canonicalUrl;
        applyFixedValue(resource, "url", "https://fhir.cognovis.de/dental-core/StructureDefinition/recorded-apical-finding");
        return new RecordedApicalFindingExtProfile(resource);
    }

    private static resolveInput (args: RecordedApicalFindingExtProfileRaw | RecordedApicalFindingExtProfileFlat) : Extension[] {
        if (isRawExtensionInput<RecordedApicalFindingExtProfileRaw>(args)) {
            return args.extension ?? [];
        } else {
            const result: Extension[] = [];
            if (args.description !== undefined) {
                result.push({ url: "description", valueString: args.description } as Extension);
            }
            if (args.root !== undefined) {
                result.push({ url: "root", valueString: args.root } as Extension);
            }
            return result;
        }
    }

    static createResource (args: RecordedApicalFindingExtProfileRaw | RecordedApicalFindingExtProfileFlat) : Extension {
        const resolvedExtensions = RecordedApicalFindingExtProfile.resolveInput(args);

        const resource: Extension = {
            url: "https://fhir.cognovis.de/dental-core/StructureDefinition/recorded-apical-finding",
            extension: resolvedExtensions,
        }
        return resource;
    }

    static create (args: RecordedApicalFindingExtProfileRaw | RecordedApicalFindingExtProfileFlat) : RecordedApicalFindingExtProfile {
        return RecordedApicalFindingExtProfile.apply(RecordedApicalFindingExtProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined;
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    // Extensions
    public setDescription (value: string): this {
        upsertExtension(this.resource, { url: "description", valueString: value } as Extension)
        return this
    }

    public getDescription(mode: 'flat'): string | undefined;
    public getDescription(mode: 'raw'): Extension | undefined;
    public getDescription(): string | undefined;
    public getDescription (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "description")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueString")
    }

    public setRoot (value: string): this {
        upsertExtension(this.resource, { url: "root", valueString: value } as Extension)
        return this
    }

    public getRoot(mode: 'flat'): string | undefined;
    public getRoot(mode: 'raw'): Extension | undefined;
    public getRoot(): string | undefined;
    public getRoot (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "root")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueString")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "RecordedApicalFindingExt"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "extension"),
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", RecordedApicalFindingExtProfile.canonicalUrl),
            ],
            warnings: [],
        }
    }

}
