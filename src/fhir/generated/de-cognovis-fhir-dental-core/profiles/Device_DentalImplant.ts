// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { CodeableConcept } from "../../hl7-fhir-r4-core/CodeableConcept";
import type { Coding } from "../../hl7-fhir-r4-core/Coding";
import type { Device } from "../../hl7-fhir-r4-core/Device";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Identifier } from "../../hl7-fhir-r4-core/Identifier";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";

import { ToothPositionExtProfile } from "./Extension_ToothPositionExt";

import {
    ensureProfile,
    isExtension,
    getExtensionValue,
    pushExtension,
    upsertExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateSliceFields,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateChoiceProhibited,
    validateMustSupport,
} from "../../profile-helpers";

export type DentalImplantProfileRaw = {
    extension: Extension[];
    identifier: Identifier[];
    status: ("active" | "inactive" | "entered-in-error" | "unknown");
    type: CodeableConcept;
    patient: Reference<"Patient">;
}

// CanonicalURL: https://fhir.cognovis.de/dental-core/StructureDefinition/dental-implant (pkg: de.cognovis.fhir.dental.core#0.6.0)
export class DentalImplantProfile {
    static readonly canonicalUrl = "https://fhir.cognovis.de/dental-core/StructureDefinition/dental-implant";

    private resource: Device;

    constructor (resource: Device) {
        this.resource = resource;
    }

    static from (resource: Device) : DentalImplantProfile {
        if (!resource.meta?.profile?.includes(DentalImplantProfile.canonicalUrl)) {
            throw new Error(`DentalImplantProfile: meta.profile must include ${DentalImplantProfile.canonicalUrl}`)
        }
        const profile = new DentalImplantProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static is (resource: unknown) : resource is Device {
        if (typeof resource !== "object" || resource === null) return false;
        const r = resource as { resourceType?: string; meta?: { profile?: string[] } };
        if (r.resourceType !== "Device") return false;
        return (r.meta?.profile ?? []).includes(DentalImplantProfile.canonicalUrl);
    }

    static apply (resource: Device) : DentalImplantProfile {
        ensureProfile(resource, DentalImplantProfile.canonicalUrl);
        return new DentalImplantProfile(resource);
    }

    static createResource (args: DentalImplantProfileRaw) : Device {
        const resource: Device = {
            resourceType: "Device",
            extension: args.extension,
            identifier: args.identifier,
            status: args.status,
            type: args.type,
            patient: args.patient,
            meta: { profile: [DentalImplantProfile.canonicalUrl] },
        }
        return resource;
    }

    static create (args: DentalImplantProfileRaw) : DentalImplantProfile {
        const resource = DentalImplantProfile.createResource(args);
        return DentalImplantProfile.apply(resource);
    }

    toResource () : Device {
        return this.resource;
    }

    // Field accessors
    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined;
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value });
        return this;
    }

    getIdentifier () : Identifier[] | undefined {
        return this.resource.identifier as Identifier[] | undefined;
    }

    setIdentifier (value: Identifier[]) : this {
        Object.assign(this.resource, { identifier: value });
        return this;
    }

    getStatus () : ("active" | "inactive" | "entered-in-error" | "unknown") | undefined {
        return this.resource.status as ("active" | "inactive" | "entered-in-error" | "unknown") | undefined;
    }

    setStatus (value: ("active" | "inactive" | "entered-in-error" | "unknown")) : this {
        Object.assign(this.resource, { status: value });
        return this;
    }

    getType () : CodeableConcept | undefined {
        return this.resource.type as CodeableConcept | undefined;
    }

    setType (value: CodeableConcept) : this {
        Object.assign(this.resource, { type: value });
        return this;
    }

    getPatient () : Reference<"Patient"> | undefined {
        return this.resource.patient as Reference<"Patient"> | undefined;
    }

    setPatient (value: Reference<"Patient">) : this {
        Object.assign(this.resource, { patient: value });
        return this;
    }

    // Extensions
    public setToothPosition (value: ToothPositionExtProfile | Extension | Coding): this {
        if (value instanceof ToothPositionExtProfile) {
            upsertExtension(this.resource, value.toResource())
        } else if (isExtension(value)) {
            if (value.url !== "https://fhir.cognovis.de/dental-core/StructureDefinition/tooth-position") throw new Error(`Expected extension url 'https://fhir.cognovis.de/dental-core/StructureDefinition/tooth-position', got '${value.url}'`)
            upsertExtension(this.resource, value)
        } else {
            upsertExtension(this.resource, { url: "https://fhir.cognovis.de/dental-core/StructureDefinition/tooth-position", valueCoding: value as Coding } as Extension)
        }
        return this
    }

    public getToothPosition(mode: 'flat'): Coding | undefined;
    public getToothPosition(mode: 'profile'): ToothPositionExtProfile | undefined;
    public getToothPosition(mode: 'raw'): Extension | undefined;
    public getToothPosition(): Coding | undefined;
    public getToothPosition (mode: 'flat' | 'profile' | 'raw' = 'flat'): Coding | ToothPositionExtProfile | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "https://fhir.cognovis.de/dental-core/StructureDefinition/tooth-position")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        if (mode === 'profile') return ToothPositionExtProfile.apply(ext)
        return getExtensionValue<Coding>(ext, "valueCoding")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "DentalImplant"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "extension"),
                ...validateRequired(res, profileName, "identifier"),
                ...validateRequired(res, profileName, "status"),
                ...validateEnum(res, profileName, "status", ["active","inactive","entered-in-error","unknown"]),
                ...validateRequired(res, profileName, "type"),
                ...validateRequired(res, profileName, "patient"),
                ...validateReference(res, profileName, "patient", ["Patient"]),
            ],
            warnings: [],
        }
    }

}
