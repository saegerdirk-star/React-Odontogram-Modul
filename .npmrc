# The Cognovis registry hosts @cognovis packages used by the Aidbox live-mode
# dev app and the Dental Core generator (devDependencies). Authentication comes
# from the developer's own ~/.npmrc — never put a token in this file.
@cognovis:registry=https://npm.cognovis.de
