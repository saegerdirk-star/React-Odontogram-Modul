# === Node / React ===
node_modules/
.codegen-cache/
test_uis/odontogram_editor_v4/node_modules
backend/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# === Build output ===
build/
dist/
out/

# Derived `tsc -b` incremental cache — rewritten by every build, never committed.
tsconfig.tsbuildinfo

# === Env files ===
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# === OS ===
.DS_Store
Thumbs.db

# === Editor / IDE ===
.vscode/
.idea/
.claude/
*.swp
*.swo

# === Testing / coverage ===
coverage/

# === Python bytecode cache ===
# Written by tools/toothgen on every generator or verification run.
__pycache__/
*.py[cod]

# === Misc ===
*.log
.intake/

# === Adobe Illustrator files ===
graphics/

# === SNOMED CT working files ===
snomed/

# === Clinical source material ===
# Printouts from the practice software used to derive the orthodontic beads
# (odontogram-c51.1 model analysis, c51.2 cephalometry). They carry patient
# identity and must never enter version control - the beads reference the
# folder, never its contents.
KFO/

# === Beads runtime state ===
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
